## Packages
framer-motion | Page transitions and sleek hover animations
date-fns | Human-readable date formatting for queue and orders
recharts | Dashboard analytics charts and data visualization
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
- Using Replit Auth (use-auth.ts already exists in context)
- App forces Dark Mode for a premium professional gaming/MMO aesthetic
- API schema provides `api` and `buildUrl` exports from `@shared/routes`
- Zod schemas are available in `@shared/schema`
- Stock images used for the Auth landing page hero (Unsplash)
